---
import "../styles/global.css";
import { projects } from "../data/projects";
const base = import.meta.env.BASE_URL;
---

<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Mohamed Feituri – Portfolio</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  </head>
  <body class="bg-white text-gray-900 font-sans scroll-smooth">

    <!-- Nav -->
    <nav class="flex justify-between items-center py-6 px-10 sticky top-0 bg-white/90 backdrop-blur z-50 shadow-sm">
      <h1 class="text-xl font-bold">MF</h1>
      <ul class="flex space-x-8 text-gray-700">
        <li><a href="#about" class="hover:text-black transition">About</a></li>
        <li><a href="#projects" class="hover:text-black transition">Projects</a></li>
        <li><a href="#contact" class="hover:text-black transition">Contact</a></li>
      </ul>
    </nav>

    <!-- Hero -->
    <section class="text-center py-28 px-6">
      <img src={"selfie.jpg"} alt="Profile" class="w-32 h-32 rounded-full mx-auto mb-6 shadow-lg object-cover"/>
      <h2 class="text-5xl font-bold mb-4">Mohamed Feituri</h2>
      <p class="text-xl text-gray-600 max-w-2xl mx-auto">
        B.S. Computer Science, Colorado School of Mines | Mobile Developer (iOS & Android) | Swift, SwiftUI, Kotlin, Jetpack Compose
      </p>
    </section>

    <!-- About -->
    <section id="about" class="py-24 px-6 bg-gray-50">
      <div class="max-w-3xl mx-auto text-center">
        <h2 class="text-4xl font-bold mb-6">About Me</h2>
<p class="text-lg text-gray-700 leading-relaxed">
  I’m a mobile app developer skilled in iOS and Android development, pursuing a B.S. in Computer Science at the Colorado School of Mines. I enjoy building user-focused apps with SwiftUI, UIKit, and Jetpack Compose, emphasizing clean architecture and impactful experiences. My recent projects include a Spotify powered iOS app (Beanify), a TikTok style Android app (Whistle), and a mountain biking app developed for a client(SoGnar). I’ve worked across the full stack, from OAuth 2.0 authentication to Firebase and location features with MapKit/MapBox. I’m always eager to collaborate, learn, and push my skills in mobile development and product design.
</p>
      </div>
    </section>

    <!-- Projects -->
    <section id="projects" class="py-24 px-6 bg-gray-50">
      <h2 class="text-4xl font-bold text-center mb-12">Projects</h2>
      <div class="grid md:grid-cols-3 gap-12 max-w-6xl mx-auto">
        {projects.map(project => (
          <a
            href={project.link || "/projects/" + project.slug}
            target="_blank"
            class="group bg-white rounded-3xl shadow-md hover:shadow-xl transition overflow-hidden block"
          >
            <img
              src={project.image}
              class="w-full h-56 object-cover rounded-t-3xl"
            />
            <div class="p-6">
              <h3 class="text-2xl font-semibold mb-2 group-hover:text-purple-500 transition">{project.title}</h3>
              <p class="text-gray-600 mb-4">{project.description}</p>
              <div class="flex flex-wrap gap-2">
                {project.tech.map(tech => (
                  <span class="px-3 py-1 bg-gray-100 text-gray-800 rounded-full text-sm">{tech}</span>
                ))}
              </div>
            </div>
          </a>
        ))}
      </div>
    </section>

<!-- Contact -->
<section id="contact" class="py-24 px-6 bg-gray-50 text-center">
  <h2 class="text-4xl font-bold mb-6">Get In Touch</h2>
  <p class="text-lg text-gray-700 mb-8">I’m always open to opportunities in mobile development or just to chat!</p>
  <a href="https://www.linkedin.com/in/mohamed-feituri/" target="_blank" 
     class="bg-blue-600 text-white px-8 py-4 rounded-xl text-lg hover:bg-blue-700 transition">
    Connect on LinkedIn
  </a>
</section>

    <!-- Footer -->
    <footer class="py-10 text-center text-gray-500 text-sm border-t">
      © 2025 Mohamed Feituri. All rights reserved.
    </footer>
  </body>
</html>
